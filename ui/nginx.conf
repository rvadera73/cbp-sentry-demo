events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  gzip on;
  gzip_types text/css application/javascript text/javascript;

  server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Add version header for debugging
    add_header X-App-Version "sentry-cbp-v1" always;

    # HTML - NO CACHE (always get latest)
    location = /index.html {
      add_header Cache-Control "no-cache, no-store, must-revalidate, max-age=0" always;
      add_header Pragma "no-cache" always;
      add_header Expires "0" always;
    }

    # React Router fallback
    location / {
      try_files $uri $uri/ /index.html;
      add_header Cache-Control "no-cache, no-store, must-revalidate, max-age=0" always;
    }

    # API Proxy - for LOCAL DOCKER development only
    # In Cloud Run, React code detects sentry-ui-*.run.app hostname and uses HTTPS public URLs instead
    location /api/ {
      proxy_pass http://sentry-api:8000;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto http;
      proxy_set_header Connection "";
      proxy_buffering off;
      proxy_request_buffering off;
    }

    # Hashed assets (Vite generates these with hash in filename)
    location ~* "\-[0-9a-f]{8}\.(js|css)$" {
      expires 1y;
      add_header Cache-Control "public, immutable" always;
    }

    # All other static assets - SHORT CACHE
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
      expires 1d;
      add_header Cache-Control "public, max-age=86400" always;
    }
  }
}
